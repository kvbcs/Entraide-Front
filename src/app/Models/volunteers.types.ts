export type Volunteers = {
    id_volunteer?: boolean;
    first_name: string;
    last_name: string;
    is_absent?: boolean;
    start_date?: string;
    end_date?: string;
    created_at?: string;
    updated_at?: string;
}